/**
 * `leaflet.gridlayer.googlemutant` ships no types. It is imported only for its side effect: it adds
 * `L.gridLayer.googleMutant` to Leaflet, which `MapCanvas.tsx` reads through a narrow cast.
 */
declare module "leaflet.gridlayer.googlemutant";
